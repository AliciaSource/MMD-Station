# Bullet Physics 2.75

Vendored from Bullet 2.75 development revision `f919baf03d404aaf088cbeb1a94098f7c045721b` for MikuMikuDance-compatible rigid body and constraint semantics.

Only `src/` is retained because the Rust build compiles the library directly. The upstream zlib license is in `LICENSE.txt`.
